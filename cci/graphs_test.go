package cci

import "testing"

func TestGraphMinimalTree(t *testing.T) {
	cases := []struct {
		name string
	}{
		{
			"given ",
		},
	}

	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {

		})
	}
}
